import { StyleSheet } from "react-native";
import { SIZES, COLORS, FONTS } from "../../../constants";

const styles = StyleSheet.create({
    
    inputStyle: {
        backgroundColor: '#f9f5ed',
        width: "70%",
        elevation : 5,
        borderRadius:5,
    },
    
    alert : {
        backgroundColor: "#a56f",
        borderColor : 'red',
        borderWidth: 2
    }

})

export default styles