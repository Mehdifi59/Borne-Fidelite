import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    btn : {
        backgroundColor:"#E66298",
        height:100,
        marginTop:50,
        width : 100,
        borderRadius: 100,
        justifyContent:"center",
        alignItems:"center",
    },
    btnImg : {
        width: '40%',
        height:"40%"
    }

})

export default styles