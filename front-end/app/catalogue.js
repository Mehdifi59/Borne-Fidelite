import { Text, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context";

const catalogue = () => {

    return(
        <SafeAreaView>
            <Text>Catalogue Boy</Text>
        </SafeAreaView>
    )
}

export default catalogue